# SA name/namespace MUST match the Pod Identity association map in
# infra/eks.tf — two-file, same-PR rename rule (docs/architecture.md).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: keycloak
  namespace: keycloak
