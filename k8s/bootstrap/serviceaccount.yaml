# One-shot Job SA. Carries its own Pod Identity association (infra/eks.tf
# workload_associations: foundry-bootstrap/db-bootstrap) — the job mounts
# the break-glass master secrets plus the service credentials it stages.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: db-bootstrap
  namespace: foundry-bootstrap
