# SA name/namespace MUST match the Pod Identity association map in
# infra/eks.tf (workload_associations) — renames are a two-file, same-PR
# change per the workload identity contract (docs/architecture.md).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: backend
  namespace: conduit
