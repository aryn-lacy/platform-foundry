# Assumed platform Prometheus (A3) — per landing zone. Patched onto both
# templates via target selector; no metadata.name needed.
apiVersion: argoproj.io/v1alpha1
kind: AnalysisTemplate
spec:
  args:
    - name: prometheus-address
      value: http://prometheus.platform-prod.svc:9090
